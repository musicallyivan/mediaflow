pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url = uri("https://maven.wso2.org/nexus/content/repositories/public/")
        }
        flatDir {
            dirs("app/libs")
        }
    }
}

rootProject.name = "MediaFlowAndroid"
include(":app")

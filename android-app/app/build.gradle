plugins {
    id "com.android.application"
}

android {
    namespace "com.musicallyivan.mediaflow"
    compileSdk 35

    defaultConfig {
        applicationId "com.musicallyivan.mediaflow"
        minSdk 29
        targetSdk 35
        versionCode 1
        versionName "1.0.0"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    def localFfmpegKit = file("libs/ffmpeg-kit-full.aar")

    if (localFfmpegKit.exists()) {
        implementation files(localFfmpegKit)
    } else {
        implementation "com.arthenica:ffmpeg-kit-full:6.0-2"
        implementation "com.arthenica:smart-exception-java9:0.2.1"
    }
}

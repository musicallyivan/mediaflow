# Third-party notices

This application uses `ffmpeg` for audio, video and image conversion.

FFmpeg is a separate open-source project. Depending on the exact build distributed with the release, FFmpeg components may be licensed under LGPL, GPL, or other compatible open-source licenses.

Project website:

https://ffmpeg.org/

Source code:

https://ffmpeg.org/download.html

When distributing builds that include `ffmpeg.exe` and `ffprobe.exe`, include the license files provided by the FFmpeg build used for that release. The GitHub release workflow copies available license and copying files from the downloaded FFmpeg package into the release ZIP when present.
